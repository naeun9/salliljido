# 살릴지도 (Salliljido)

인구감소지역에서 며칠~한 달 살기를 준비하는 사람들을 위한 안내 서비스.
2026 한국관광공사 관광데이터 활용 공모전 웹·앱 개발 부문 출품작.

## 최우선 원칙 — 디자인 보존

`design/salliljido.html`은 완성된 최종 디자인이다.
이 파일을 **변형 없이 그대로** React로 옮기는 것이 이 프로젝트의 핵심이다.

### 반드시 지킬 것
- HTML 구조(태그, 중첩, 클래스명)를 그대로 유지
- CSS 값(색상, 폰트 크기, 여백, 모서리, 그림자, 트랜지션)을
  단 하나도 임의로 바꾸지 말 것
- 레이아웃(flex/grid 설정, 비율, 정렬, sticky 동작)을 그대로
- 애니메이션과 호버 효과도 그대로

### 하지 말 것
- "더 나은" 디자인을 제안하거나 적용하지 말 것
- UI 라이브러리(MUI, shadcn, Ant 등)로 대체하지 말 것
- Tailwind로 변환하지 말 것 (원본이 순수 CSS면 CSS 유지)
- 색상·간격 값을 "정리"한다며 반올림하지 말 것
- 마크업 구조를 접근성 이유로 바꾸지 말 것 (aria 속성 추가는 허용)

### 변경이 필요해 보이면
바꾸지 말고 먼저 물어볼 것. 기능상 불가피한 경우에만
최소한으로 수정하고 무엇을 왜 바꿨는지 명시할 것.

## 기술 스택
- React + Vite
- react-router-dom (라우팅)
- Kakao Map SDK (지도)
- Vercel Serverless Functions (`api/`)
- localStorage (계획·저장 데이터)
- 상태 관리: Context + useReducer (외부 라이브러리 없이)

## 보안 규칙 (절대)
- 한국관광공사 인증키는 `api/` 서버리스 함수에서만 사용
- 프론트엔드 번들에 키가 들어가면 안 됨
- 모든 키는 환경변수로, 코드에 하드코딩 금지
- `.env.local`은 커밋하지 말 것
- 관광공사 API는 실시간 호출 방식 유지 (로컬 DB 저장 금지)

## 코드 원칙
- 한 파일 300줄을 넘기지 말고 넘으면 분리
- 컴포넌트는 단일 책임, 재사용 가능한 단위로
- 비즈니스 로직은 컴포넌트가 아닌 hooks/utils로
- 중복 코드를 만들지 말 것
- 주석은 한국어로, 왜 그렇게 했는지 위주로